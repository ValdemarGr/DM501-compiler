func sum(a: int, b: bool): bool
    if true then return !b; else return b;
    allocate a;

    return false;
end sum

func main(a: int): int
    var b : bool;
    var c : int;

    var i : bool;

    func main(z: int): int
        return 5;
    end main

    i = sum(c, b);
    c = 1 + 4;

    if true && false then
        return 5;

    return a + b;
end main

